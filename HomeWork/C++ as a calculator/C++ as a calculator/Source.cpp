#include <iostream>
#include<math.h>
using namespace std;
int main()
{
	/*
	// Find area of the triangle
	int a, b, c, p, S;

	cout << "a= ";
	cin >> a;
	cout << "b= ";
	cin >> b;
	cout << "c= ";
	cin >> c;

	p = (a + b + c) / 2;
	S = sqrt(p * (p - a) * (p - b) * (p - c));

	cout << "Area is = " << S << endl;
	*/



	system("pause");
	return 0;
}